# 🏗️ Arquitetura do Sistema - Diagrama

## 📐 Visão Geral

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENTE (React)                          │
│                  :3000 (Frontend)                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • ClienteList.js                                    │  │
│  │  • ProductoCrud.jsx                                  │  │
│  │  • EntregaCrud.jsx                                   │  │
│  │  • Dashboard.jsx                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                    HTTP Requests (JSON)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 SERVIDOR (Express.js)                       │
│                  :3001 (Backend)                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Routes (sem autenticação)               │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │  /api/clientes      [GET, POST, PUT, DEL]  │    │  │
│  │  │  /api/produtos      [GET, POST, PUT, DEL]  │    │  │
│  │  │  /api/entregas      [GET, POST, PUT, DEL]  │    │  │
│  │  │  /health            [GET]                   │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Controllers (Lógica de Negócio)            │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │ • entregaController.js                       │   │  │
│  │  │ • clienteRoutes (inline)                     │   │  │
│  │  │ • produtoRoutes (inline)                     │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Models (Sequelize ORM)                  │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐   │  │
│  │  │  Cliente   │  │  Produto   │  │  Entrega   │   │  │
│  │  └────────────┘  └────────────┘  └────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                      SQL Queries
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   BANCO DE DADOS                            │
│                    MySQL (localhost)                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Database: sistema_entregas                          │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │  Table: clientes                            │    │  │
│  │  │  ├─ id (PK)                                 │    │  │
│  │  │  ├─ nome                                    │    │  │
│  │  │  ├─ email (UNIQUE)                          │    │  │
│  │  │  ├─ telefone                                │    │  │
│  │  │  ├─ endereco                                │    │  │
│  │  │  ├─ createdAt                               │    │  │
│  │  │  └─ updatedAt                               │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │  Table: produtos                            │    │  │
│  │  │  ├─ id (PK)                                 │    │  │
│  │  │  ├─ nome                                    │    │  │
│  │  │  ├─ tipo                                    │    │  │
│  │  │  ├─ validade                                │    │  │
│  │  │  ├─ descricao                               │    │  │
│  │  │  ├─ createdAt                               │    │  │
│  │  │  └─ updatedAt                               │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │  Table: entregas                            │    │  │
│  │  │  ├─ id (PK)                                 │    │  │
│  │  │  ├─ protocolo (UNIQUE)                      │    │  │
│  │  │  ├─ status                                  │    │  │
│  │  │  ├─ clienteId (FK)                          │    │  │
│  │  │  ├─ produtoId (FK)                          │    │  │
│  │  │  ├─ createdAt                               │    │  │
│  │  │  └─ updatedAt                               │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Requisição

### Criar Cliente
```
1. Frontend (React)
   └─> Form submit
       └─> clienteService.criar({nome, email, ...})
           └─> axios.post('/api/clientes', data)

2. Rede
   └─> HTTP POST http://localhost:3001/api/clientes

3. Backend (Express)
   └─> router.post('/', async (req, res))
       └─> Validação de campos
           └─> Cliente.create(req.body)
               └─> Database insert

4. Banco de Dados (MySQL)
   └─> INSERT INTO clientes (nome, email, ...)
       └─> RETURN id, createdAt, updatedAt

5. Resposta
   └─> res.status(201).json(cliente)
       └─> Frontend recebe: {id: 1, nome: "João", ...}
           └─> Atualiza lista localmente
```

## 🔀 Fluxo Completo do CRUD

```
┌──────────────────────────────────────────────────────┐
│                 LISTAR (GET)                         │
├──────────────────────────────────────────────────────┤
│ Frontend: useEffect(() => {                          │
│   api.get('/api/clientes')                           │
│     .then(res => setClientes(res.data))              │
│ })                                                   │
│                                                      │
│ Backend:                                             │
│ router.get('/', async (req, res) => {               │
│   const clientes = await Cliente.findAll()           │
│   res.json(clientes)                                 │
│ })                                                   │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                 CRIAR (POST)                         │
├──────────────────────────────────────────────────────┤
│ Frontend: handleSubmit = (data) => {                 │
│   api.post('/api/clientes', data)                    │
│     .then(() => refetch())                           │
│ }                                                    │
│                                                      │
│ Backend:                                             │
│ router.post('/', async (req, res) => {              │
│   validate(req.body) // Campos obrigatórios          │
│   const cliente = await Cliente.create(req.body)     │
│   res.status(201).json(cliente)                      │
│ })                                                   │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                 ATUALIZAR (PUT)                      │
├──────────────────────────────────────────────────────┤
│ Frontend: handleEdit = (id, data) => {               │
│   api.put(`/api/clientes/${id}`, data)               │
│     .then(() => refetch())                           │
│ }                                                    │
│                                                      │
│ Backend:                                             │
│ router.put('/:id', async (req, res) => {            │
│   const cliente = await Cliente.findByPk(id)         │
│   await cliente.update(req.body)                     │
│   res.json(cliente)                                  │
│ })                                                   │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                 DELETAR (DELETE)                     │
├──────────────────────────────────────────────────────┤
│ Frontend: handleDelete = (id) => {                   │
│   api.delete(`/api/clientes/${id}`)                  │
│     .then(() => refetch())                           │
│ }                                                    │
│                                                      │
│ Backend:                                             │
│ router.delete('/:id', async (req, res) => {         │
│   const cliente = await Cliente.findByPk(id)         │
│   await cliente.destroy()                            │
│   res.status(204).send()                             │
│ })                                                   │
└──────────────────────────────────────────────────────┘
```

## 🏛️ Estrutura de Pastas - Completa

```
projeto-cadastro/
│
├── backend/                        # API Node.js/Express
│   ├── config/
│   │   ├── config.js              # Configuração geral
│   │   └── database.js            # Sequelize config
│   │
│   ├── controllers/
│   │   └── entregaController.js   # Lógica de entregas
│   │
│   ├── models/
│   │   ├── index.js               # Carregador de modelos
│   │   ├── Cliente.js             # Modelo Cliente
│   │   ├── Produto.js             # Modelo Produto
│   │   ├── Entrega.js             # Modelo Entrega
│   │   └── dbConnect.js            # (não usado)
│   │
│   ├── routes/
│   │   ├── clienteRoutes.js       # Rotas de clientes
│   │   ├── produtoRoutes.js       # Rotas de produtos
│   │   └── entregaRoutes.js       # Rotas de entregas
│   │
│   ├── middleware/
│   │   └── auth.js                # (deprecated - sem uso)
│   │
│   ├── database/
│   │   └── db.js                  # Conexão MySQL
│   │
│   ├── server.js                  # Entry point ✨
│   ├── test-api.js                # Testes automáticos
│   ├── package.json
│   ├── .env.example               # Exemplo de config
│   ├── README.md                  # Documentação
│   ├── API_DOCS.md                # Endpoints
│   └── db.json                    # (não usado)
│
├── frontend/                       # React SPA
│   ├── public/
│   │   ├── index.html
│   │   └── manifest.json
│   │
│   ├── src/
│   │   ├── components/
│   │   │   ├── ClienteList.js
│   │   │   ├── EntregaList.js
│   │   │   ├── ClienteCrud.jsx
│   │   │   ├── EntregaCrud.jsx
│   │   │   ├── ProdutoCrud.jsx
│   │   │   ├── Home.jsx
│   │   │   ├── Navbar.js
│   │   │   ├── PrivateRoute.js
│   │   │   ├── Login.js
│   │   │   ├── Register.js
│   │   │   └── template/          # Layout components
│   │   │
│   │   ├── services/
│   │   │   ├── api.js             # Axios instance ✨
│   │   │   └── auth.js            # (pode remover)
│   │   │
│   │   ├── contexts/
│   │   │   └── AuthContext.js      # (pode simplificar)
│   │   │
│   │   ├── layouts/
│   │   │   ├── AuthLayout.jsx
│   │   │   └── MainLayout.jsx
│   │   │
│   │   ├── main/
│   │   │   ├── App.jsx
│   │   │   └── Routes.jsx
│   │   │
│   │   ├── App.js
│   │   ├── index.js
│   │   ├── router.jsx
│   │   └── Routes.js
│   │
│   ├── package.json
│   ├── webpack.config.js
│   └── README.md
│
├── REFACTORING_SUMMARY.md         # Mudanças realizadas ✨
├── IMPLEMENTATION_CHECKLIST.md    # Checklist completo ✨
└── FRONTEND_INTEGRATION.md        # Guia de integração ✨
```

## 🔗 Relacionamentos de Dados

```
         ┌─────────────┐
         │   Cliente   │
         ├─────────────┤
         │ id (PK)     │
         │ nome        │
         │ email       │
         │ telefone    │
         │ endereco    │
         └─────────────┘
              △
              │
              │ (1:N)
              │
              │
         ┌─────────────┐
         │  Entrega    │
         ├─────────────┤
         │ id (PK)     │
         │ protocolo   │
         │ status      │
         │ clienteId──►│
         │ produtoId───┼────┐
         └─────────────┘    │
                            │
                            │ (1:N)
                            │
                            ▼
                      ┌──────────────┐
                      │   Produto    │
                      ├──────────────┤
                      │ id (PK)      │
                      │ nome         │
                      │ tipo         │
                      │ validade     │
                      │ descricao    │
                      └──────────────┘

Interpretação:
• Um Cliente pode ter MUITAS Entregas
• Um Produto pode estar em MUITAS Entregas
• Uma Entrega tem UM Cliente e UM Produto
```

## 📊 Ciclo de Vida da Requisição

```
┌─────────────────────────────────────────────────────┐
│                   INÍCIO                            │
└─────────────────────────────────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Frontend (React)      │
           │  Usuário clica botão   │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Service (axios)       │
           │  Monta requisição      │
           └────────────────────────┘
                        │
                   HTTP POST
                   JSON body
                        │
                        ▼
           ┌────────────────────────┐
           │  CORS Middleware       │
           │  Valida origem         │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Parser Middleware     │
           │  JSON → req.body       │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Route Handler         │
           │  Valida dados          │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Controller/Model      │
           │  Sequelize query       │
           └────────────────────────┘
                        │
                   SQL INSERT
                        │
                        ▼
           ┌────────────────────────┐
           │  MySQL Database        │
           │  Salva registro        │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  Controller/Model      │
           │  Retorna dados criados │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  res.status(201).json()│
           │  Envia resposta        │
           └────────────────────────┘
                        │
                   HTTP 201
                   JSON response
                        │
                        ▼
           ┌────────────────────────┐
           │  Frontend recebe       │
           │  .then(res => ...)     │
           └────────────────────────┘
                        │
                        ▼
           ┌────────────────────────┐
           │  React setState        │
           │  Atualiza UI           │
           └────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────┐
│                     FIM                             │
└─────────────────────────────────────────────────────┘
```

## 🎯 Status Codes HTTP Esperados

```
✅ 200 OK              - GET, PUT (sucesso)
✅ 201 CREATED         - POST (criado com sucesso)
✅ 204 NO CONTENT      - DELETE (deletado com sucesso)
❌ 400 BAD REQUEST     - Validação falhou
❌ 404 NOT FOUND       - Recurso não existe
❌ 500 SERVER ERROR    - Erro no servidor
```

## 🔒 Segurança Atual

```
✅ CORS habilitado
✅ Helmet ativado
✅ Validação de entrada
✅ Tratamento de erro

❌ Sem autenticação (pode adicionar depois)
❌ Sem autorização por papéis
❌ Sem rate limiting
❌ Sem HTTPS (local)
```

---

**Última atualização**: Dezembro 2024
**Versão**: 1.0.0
